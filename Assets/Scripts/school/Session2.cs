using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Session : MonoBehaviour
{


    /*public int myNumber = 2;
    public bool Questiontime = false;
    int myValuablQuestiontime;


    string[] fruits = { "apple", "grapes", "blueberry" };
    List<int> evennumbers = new List<int>();
    int[] evennumbersSmart = new int[10];*/

    
                      
        }
                      
   
  


    // Use this for initialization
    /*void Start()
    {
        myValuablQuestiontime = (Questiontime == true) ? 1 : 0;

        Debug.Log("The value if myVariableQuestiontime is:" + myVariableQuestiontime);

        if (myNumber == 2 && Questiontime == false)
        {
            Debug.Log("your number is 2 and QT is false");

        }

        //for loop   start value how this ends 
        for (int i = 0; i <= fruits. length; i = i + 1)
        {
            Debug.Log("Fruit at position" + i + "is" + fruits[1]);
        }
        
        for (int i=0; i<20; i = i+2)
        {
            evennumbers,Add(i);
            evennumbersSmart[i / 2] = i;
        
    }
        for(int i=0, i < evennumbers.Count; i++)
        {
            Debug.Log("myNumber is" + evennumbers[i]);
        }

        List<int> myonehundredNumber = new List<int>();
        for (int i = 0; 1 < 100; i++)
        {
            myonehundredNumber.Add(i);

        }
        for(int i =0; i < myonehundredNumber.Count; i = i + 5)
        {
            Debug.Log("Numbers divisible by 5;" + myonehundredNumber[i]);

        }

       // List<int> oddnumbers = new List<int>();
       // int counter = 1;
        //while (counter < 100)
        {
       // Debug.Log("Odd number : " + oddnumbers[i]);
        }

    }

// Update is called once per frame
void Update()
{
    if (myNumber == 2)
    {
        Debug.Log("Your number is equal to 2,");
    }
    else
    {
        Debug.Log("Your number is not equal to 2,");
    }
    if (questiontime == false)
    {
        Debug.Log("Question time is true");
    }
    else
    {
        Debug.Log("Question time is false"); } };*/

    

       